{%- comment -%}
  Renders a product_group block's promo banner.
  Usage: {% render 'mcg-promo', block: block %}
{%- endcomment -%}

{%- if block.settings.promo_text != blank -%}
  <div class="mcg__promo">
    <div class="mcg__promo-desktop">
      {{ block.settings.promo_text }}
    </div>

    <div class="mcg__promo-mobile">
      {%- for i in (1..3) -%}
        {%- assign image_key = 'promo_image_' | append: i -%}
        {%- if block.settings[image_key] != blank -%}
          <div class="mcg__promo-slide">
            {{ block.settings[image_key] | image_url: width: 200 | image_tag: loading: 'eager' }}
          </div>
        {%- endif -%}
      {%- endfor -%}

      <div class="mcg__promo-mobile-text">
        {{ block.settings.promo_text }}
      </div>
    </div>
  </div>
{%- endif -%}